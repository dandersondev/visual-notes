{
  "name": "visual-notes",
  "version": "1.1.32",
  "description": "Visual Notes — a visual workspace for Obsidian",
  "main": "main.js",
  "scripts": {
    "generate-templates": "node scripts/generate-starter-templates.mjs",
    "generate-benchmarks": "node scripts/generate-benchmark-boards.mjs",
    "predev": "npm run generate-templates",
    "dev": "node esbuild.config.mjs",
    "pretypecheck": "npm run generate-templates",
    "typecheck": "tsc --noEmit -p .",
    "pretest": "npm run generate-templates",
    "test": "vitest run",
    "prelint": "npm run generate-templates",
    "lint": "eslint src",
    "sync-types": "node scripts/vendor-types.mjs",
    "build": "npm run typecheck && npm run typecheck:upstream && npm run lint && node esbuild.config.mjs production",
    "typecheck:upstream": "tsc --noEmit -p tsconfig.build.json"
  },
  "keywords": [],
  "author": "Daniel Anderson",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/dandersondev/visual-notes.git"
  },
  "bugs": {
    "url": "https://github.com/dandersondev/visual-notes/issues"
  },
  "homepage": "https://github.com/dandersondev/visual-notes#readme",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/sortablejs": "^1.15.9",
    "@vitest/coverage-v8": "^4.1.10",
    "esbuild": "^0.28.1",
    "eslint": "^10.8.0",
    "jsdom": "^29.1.1",
    "obsidian": "1.13.0",
    "tslib": "^2.6.3",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.65.0",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "html-to-image": "^1.11.13",
    "perfect-freehand": "^1.2.3",
    "sortablejs": "^1.15.7"
  }
}
